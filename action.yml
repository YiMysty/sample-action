name: 'sample-action'
description: 'A sample action.'
inputs:
  who-to-greet:  # id of input
    description: 'Who to greet'
    required: true
    default: 'World'
runs:
  using: "composite"
  steps:
    - run: echo My version is ${{ github.action_ref }}
      shell: bash
